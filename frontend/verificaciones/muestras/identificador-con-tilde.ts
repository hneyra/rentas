// Viola: sin tildes ni enie en identificadores (CLAUDE.md §Idioma).
//
// El texto que ve el ciudadano SI lleva tildes —va en las cadenas—; lo que no las lleva es
// el identificador, porque un `grep` sobre `alicuota` tiene que encontrarlas todas.

export const alícuota = '0.006';

export function añoDeEmision() {
  return 2026;
}
