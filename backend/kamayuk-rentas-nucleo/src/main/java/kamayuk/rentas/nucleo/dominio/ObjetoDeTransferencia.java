package kamayuk.rentas.nucleo.dominio;

/** Que se transfiere. Los dos valores son los del {@code CHECK} de {@code transferencia} (V2). */
public enum ObjetoDeTransferencia {
    PREDIO,
    VEHICULO
}
